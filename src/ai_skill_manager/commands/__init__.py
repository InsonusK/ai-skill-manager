"""CLI commands package.

Each command is a subpackage containing ``api.py``, ``formatter.py`` and
``cli.py`` so that business logic can be reused independently of console
output.

Каждая команда — это подпакет, содержащий ``api.py``, ``formatter.py`` и
``cli.py``, чтобы бизнес-логику можно было переиспользовать независимо от
консольного вывода.
"""
